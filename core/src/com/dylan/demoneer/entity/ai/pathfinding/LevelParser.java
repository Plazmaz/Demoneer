package com.dylan.demoneer.entity.ai.pathfinding;

import com.badlogic.gdx.maps.tiled.TiledMap;
import com.badlogic.gdx.maps.tiled.TiledMapTileLayer;
import com.badlogic.gdx.utils.Array;
import com.sun.corba.se.impl.orbutil.graph.GraphImpl;
import com.sun.corba.se.impl.orbutil.graph.Node;

/**
 * Created by Dylan on 2/7/2016.
 */
public class LevelParser {
   /* public static GraphImpl generateGraphFromLevel(TiledMap map) {
        Array<Node> nodes = new Array<>();
        TiledMapTileLayer obstacles = (TiledMapTileLayer) map.getLayers().get("obstructions");
        int width = map.getProperties().get("width", Integer.class);
        int height = map.getProperties().get("height", Integer.class);
        for (int x = 0; x < width; ++x) {
            for (int y = 0; y < height; ++y) {
            }
        }
    }*/
}
